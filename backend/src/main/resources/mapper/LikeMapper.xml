<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.raisetimeline.mapper.LikeMapper">

    <!-- UNIQUE(post_id, user_id)違反をON CONFLICT DO NOTHINGで吸収する(PostgreSQL用)。 -->
    <insert id="insertIfAbsent" databaseId="postgresql">
        INSERT INTO likes (post_id, user_id, created_at)
        VALUES (#{postId}, #{userId}, NOW())
        ON CONFLICT (post_id, user_id) DO NOTHING
    </insert>

    <!--
        H2はON CONFLICT句自体を未サポート(2.3系で確認)のため、H2ネイティブのUPSERT構文MERGE INTOで代替する。
        KEY(post_id, user_id)が一致する行があればcreated_atを上書きするだけになりDO NOTHINGと
        完全に同一ではないが、本テストで検証する範囲(countの変化なし)には影響しないためテスト専用として許容する。
    -->
    <insert id="insertIfAbsent" databaseId="h2">
        MERGE INTO likes (post_id, user_id, created_at)
        KEY (post_id, user_id)
        VALUES (#{postId}, #{userId}, NOW())
    </insert>

    <delete id="delete">
        DELETE FROM likes WHERE post_id = #{postId} AND user_id = #{userId}
    </delete>

    <select id="countByPost" resultType="int">
        SELECT COUNT(*) FROM likes WHERE post_id = #{postId}
    </select>

</mapper>
