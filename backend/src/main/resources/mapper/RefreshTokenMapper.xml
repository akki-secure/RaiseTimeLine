<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.raisetimeline.mapper.RefreshTokenMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO refresh_tokens (user_id, token_hash, expires_at)
        VALUES (#{userId}, #{tokenHash}, #{expiresAt})
    </insert>

    <!--
        有効なトークンのみを原子的に失効させ、失効させたレコードを返す。
        SELECTでの検証とUPDATEでの失効を分けないことで、同一トークンへの同時リクエストが
        どちらも「有効」と判定してしまう競合(TOCTOU)を防ぐ。RETURNINGで結果を受け取るため
        MyBatis上は<update>ではなく<select>として扱う。
        H2はRETURNING句を未サポートのためMapper単体テスト(H2実SQL実行)の対象外とし、
        有効/期限切れ/失効済みの分岐はAuthServiceTestでMockitoにより戻り値をスタブして検証する。
    -->
    <select id="revokeIfValid" resultType="com.raisetimeline.model.RefreshToken">
        UPDATE refresh_tokens SET revoked_at = NOW()
        WHERE token_hash = #{tokenHash} AND revoked_at IS NULL AND expires_at > NOW()
        RETURNING id, user_id, token_hash, expires_at, revoked_at, created_at
    </select>

    <update id="revokeByTokenHash">
        UPDATE refresh_tokens SET revoked_at = NOW()
        WHERE token_hash = #{tokenHash} AND revoked_at IS NULL
    </update>

</mapper>
