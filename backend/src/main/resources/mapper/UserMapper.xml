<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.raisetimeline.mapper.UserMapper">

    <select id="findByEmail" resultType="com.raisetimeline.model.User">
        SELECT id, email, password_hash, username, display_name, bio, avatar_url, created_at, updated_at
        FROM users
        WHERE email = #{email}
    </select>

    <select id="findById" resultType="com.raisetimeline.model.User">
        SELECT id, email, password_hash, username, display_name, bio, avatar_url, created_at, updated_at
        FROM users
        WHERE id = #{id}
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM users WHERE email = #{email})
    </select>

    <select id="existsByUsername" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM users WHERE username = #{username})
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (email, password_hash, username, display_name)
        VALUES (#{email}, #{passwordHash}, #{username}, #{displayName})
    </insert>

</mapper>
